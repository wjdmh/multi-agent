# 11주차 실습 기록

## GitHub 저장소
- URL: https://github.com/wjdmh/multi-agent

## 내 에이전트 주제
- 에이전트 이름: 시니어 스포츠·체육 개인화 학습 파트너 에이전트
- 해결하려는 문제: 쏟아지는 시니어 스포츠/체육 관련 논문과 뉴스 중 핵심만 선별하여 사용자에게 브리핑 형태로 요약 전달.
- 입력: 논문 및 뉴스 형태의 문자열 텍스트 (제목, 초록/내용, 키워드 포함)
- 출력: 오늘의 논문 1편, 뉴스 1~2편이 선정된 구조화된 브리핑 텍스트

## 일정공지 예시에서 바꾼 점
| 일정공지 예시 | 내 에이전트 |
|---|---|
| 공지문 | 스포츠·체육 관련 논문/뉴스 원문 데이터 |
| 날짜/대상/준비물 추출 | 논문/뉴스 타입, 제목, 내용(초록), 키워드 추출 |
| 학년별 분류 | '논문'과 '뉴스'로 분류하고 키워드 매칭 관련성 점수로 정렬하여 오늘의 픽 선정 |
| 사용자별 안내문 | 한 줄 결론과 실전 적용이 포함된 시니어 스포츠 브리핑 출력 |

## 사용한 도구
- Copilot / Gemini CLI / Antigravity 중 사용한 도구: Antigravity

## 내가 준 주요 프롬프트
```text
이 프로젝트를 검토 이해한다. 오늘은 week-11.md 작업을 수행한다. 응답은 한국어로 하고 설명은 이해하기 쉽게 한다.

현재 context.md와 todo.md가 다른 맥락으로 적혀있을 텐데, 그 두가지도 수정한 뒤 my_agent.py의 첫 버전을 만들어야 한다.

내가 만들려고 하는 에이전트는 아래와 같아.
매일 아침 시니어 스포츠·체육 관련 논문·뉴스를 자동 수집·요약해서 전달하고, 대화를 통해 무현의 관심사와 학습 패턴을 누적 파악해 점점 개인화되는 학습 파트너 에이전트.
```

## 코딩에이전트가 확인한 항목
| 확인 항목 | 통과 여부 | 근거 |
|---|---|---|
| 함수가 3개 역할로 나뉘었는가 | 통과 | `extract_facts`, `classify_items`, `write_output` 로 명확하게 분리됨 |
| 앞 단계 출력이 다음 단계 입력으로 들어가는가 | 통과 | `main()`에서 `facts = extract_facts()`, `grouped = classify_items(facts)`, `result = write_output(grouped)`로 연결됨 |
| 중간 결과가 출력되는가 | 통과 | `print("=== facts ===")`, `print("=== grouped ===")` 등 중간 결과가 모두 터미널에 출력됨 |

## 실행 결과
- 실행 명령: `python3 my_agent.py`
- 성공 여부: 성공
- 오류가 있었다면 오류 내용: 없음 (`python` 명령어 대신 `python3` 사용)

## 12주차에 고칠 점
- [ ] 관련성 점수 부여(키워드 매칭 등) 로직을 더 정교하게 다듬기
- [ ] 브리핑 결과를 터미널 출력이 아니라 `output.md` 파일로 저장하도록 변경하기
- [ ] 단어 자르기가 아닌 초록 분석 기반의 '한 줄 결론' 요약 도입하기

## 12주차에 붙여 보고 싶은 확장
- Groq API 사용 여부: 사용할 예정 (LLM을 활용해 논문 초록을 제대로 된 한 줄 결론과 실전 적용 팁으로 요약하기 위해)
- 외부 도구/API 사용 여부: 추후 PubMed API 및 뉴스 RSS 연동 고려 중

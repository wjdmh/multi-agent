# AGENTS.md

이 파일은 GitHub Copilot, Gemini CLI, Antigravity에서 공통으로 참고할 프로젝트 규칙이다. 무료 사용량이 남아 있는 도구를 번갈아 쓰더라도 같은 규칙으로 작업한다.

## 프로젝트

- 이름: 시니어 스포츠·체육 개인화 학습 파트너 에이전트 (Senior Sports Learning Partner Agent)
- 목표: 매일 아침 시니어 스포츠·체육 관련 논문·뉴스를 자동 수집·요약해서 브리핑하고, 사용자와의 상호작용을 통해 관심사와 학습 패턴을 파악하여 점진적으로 개인화된 정보를 제공한다.
- 기본 실행: `python3 my_agent.py`
- 기본 작업 파일: `my_agent.py`
- 입력 예시: 논문/뉴스 원문 텍스트 (추후 PubMed RSS, 뉴스 RSS 등 API 연동)
- 주요 출력 파일: 터미널 출력 및 `output.md` (오늘의 브리핑), 대화 히스토리 JSON 파일

## 에이전트 역할

1. **수집 에이전트 (Data Agent)**: PubMed API, 뉴스 RSS 등을 통해 시니어 스포츠 관련 최신 논문 및 기사 원문을 수집하고 기본 핵심 정보(제목, 초록, 키워드 등)를 추출한다.
2. **선정 에이전트 (Curation Agent)**: 추출된 키워드와 내용을 분석해 관련성 점수를 매기고, 오늘의 최우선 자료(논문 1편, 뉴스 1~2편)를 선정한다.
3. **요약 에이전트 (Summarization Agent)**: 선정된 자료의 초록 및 내용을 기반으로 '한 줄 결론', '연구 방법', '실전 적용' 등이 포함된 구조화된 브리핑 텍스트로 요약 및 포맷팅한다.
4. **개인화 에이전트 (Personalization Agent)**: 대화 히스토리(JSON)를 누적 저장하고 관리하여, 향후 수집 및 선정 단계에서 사용자 맞춤형 가중치를 부여할 수 있도록 돕는다.

## 작업 방식

1. 파일을 수정하기 전에 먼저 계획을 제시한다.
2. 한 번에 많은 파일을 바꾸지 않는다.
3. 기존 구조를 유지하면서 작은 단위로 고친다.
4. 오류가 나면 새 기능을 추가하지 말고 오류만 고친다.
5. 변경 후 실행 명령과 확인 결과를 보고한다.

## 금지

- 불필요한 패키지 설치
- API 키 하드코딩 (반드시 환경 변수나 .env 활용할 것)
- 사용자가 요청하지 않은 대규모 리팩터링
- Playwright MCP 연결 (브라우저 확인이 필요하면 Playwright CLI 사용)

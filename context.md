# 시니어 스포츠·체육 관련 개인화 학습 파트너 에이전트

## 목표
매일 아침 시니어 스포츠·체육 관련 논문·뉴스를 자동 수집·요약해서 전달하고, 대화를 통해 사용자의 관심사와 학습 패턴을 누적 파악해 점점 개인화되는 학습 파트너 에이전트를 구축한다.

## 입력
- PubMed RSS + 뉴스 RSS 형태의 텍스트 데이터 (문자열 형태)
- 각 자료에는 논문/뉴스 구분, 제목, 초록/내용, 키워드 등의 정보가 포함되어 있음

## 출력
- 관련성 점수에 따라 선정된 **오늘의 논문 1편 + 뉴스 1~2개**
- 한 줄 결론, 연구 방법/핵심 결과, 실전 적용 등이 포함된 구조화된 **브리핑 텍스트**
- 대화 히스토리 (JSON 형식 저장 예정)

## 에이전트 역할 (v0 기준 함수 3개)
1. **`extract_facts` (수집 및 추출)**: 입력된 문자열 텍스트에서 각 기사/논문의 종류, 제목, 내용, 키워드를 딕셔너리로 추출한다.
2. **`classify_items` (선정 및 분류)**: 논문과 뉴스를 분류하고, 추출된 키워드나 내용을 기반으로 관련성 점수를 계산하여 가장 유용한 '오늘의 픽'을 선정한다.
3. **`write_output` (요약 및 전달)**: 선정된 자료들을 읽기 쉽게 포맷팅하여 브리핑 출력을 완성한다.

## 제약 및 규칙
- 이번 v0 버전에서는 외부 API(Groq, PubMed 등)를 사용하지 않고 순수 파이썬 문자열 및 규칙 기반(Rule-based)으로 구현한다.
- 복잡한 딥러닝이나 LangGraph는 향후 주차로 미룬다.
